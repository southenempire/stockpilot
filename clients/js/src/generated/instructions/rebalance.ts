/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI16Decoder,
  getI16Encoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/program-client-core";
import { STOCKPILOT_PROGRAM_ADDRESS } from "../programs";

export const REBALANCE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  108, 158, 77, 9, 210, 52, 88, 62,
]);

export function getRebalanceDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REBALANCE_DISCRIMINATOR);
}

export type RebalanceInstruction<
  TProgram extends string = typeof STOCKPILOT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type RebalanceInstructionData = {
  discriminator: ReadonlyUint8Array;
  driftBps: Array<number>;
};

export type RebalanceInstructionDataArgs = { driftBps: Array<number> };

export function getRebalanceInstructionDataEncoder(): Encoder<RebalanceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["driftBps", getArrayEncoder(getI16Encoder())],
    ]),
    (value) => ({ ...value, discriminator: REBALANCE_DISCRIMINATOR }),
  );
}

export function getRebalanceInstructionDataDecoder(): Decoder<RebalanceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["driftBps", getArrayDecoder(getI16Decoder())],
  ]);
}

export function getRebalanceInstructionDataCodec(): Codec<
  RebalanceInstructionDataArgs,
  RebalanceInstructionData
> {
  return combineCodec(
    getRebalanceInstructionDataEncoder(),
    getRebalanceInstructionDataDecoder(),
  );
}

export type RebalanceInput<
  TAccountAuthority extends InstructionSignerInput = InstructionSignerInput,
  TAccountVault extends InstructionAccountInput = InstructionAccountInput,
> = {
  authority: TAccountAuthority;
  vault: TAccountVault;
  driftBps: RebalanceInstructionDataArgs["driftBps"];
};

export function getRebalanceInstruction<
  TAccountAuthority extends InstructionSignerInput,
  TAccountVault extends InstructionAccountInput,
  TProgramAddress extends Address = typeof STOCKPILOT_PROGRAM_ADDRESS,
>(
  input: RebalanceInput<TAccountAuthority, TAccountVault>,
  config?: { programAddress?: TProgramAddress },
): RebalanceInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountAuthority,
    InstructionAccountInputAddress<TAccountAuthority>
  >,
  ResolvedInstructionAccountMeta<
    TAccountVault,
    InstructionAccountInputAddress<TAccountVault>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? STOCKPILOT_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    authority: {
      value: input.authority ?? null,
      isSigner: true,
      isWritable: false,
    },
    vault: { value: input.vault ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("vault", accounts.vault),
    ],
    data: getRebalanceInstructionDataEncoder().encode(
      args as RebalanceInstructionDataArgs,
    ),
    programAddress,
  } as RebalanceInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountAuthority,
      InstructionAccountInputAddress<TAccountAuthority>
    >,
    ResolvedInstructionAccountMeta<
      TAccountVault,
      InstructionAccountInputAddress<TAccountVault>
    >
  >);
}

export type ParsedRebalanceInstruction<
  TProgram extends string = typeof STOCKPILOT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    vault: TAccountMetas[1];
  };
  data: RebalanceInstructionData;
};

export function parseRebalanceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRebalanceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), vault: getNextAccount() },
    data: getRebalanceInstructionDataDecoder().decode(instruction.data),
  };
}
